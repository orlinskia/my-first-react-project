import React from 'react';
import CalculatorCard from "../components/CalculatorCard"

const Two = () => {
    return (
        <div>
            <CalculatorCard />
        </div>
    );
};

export default Two;